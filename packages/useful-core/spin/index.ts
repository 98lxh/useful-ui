import Spin from './src'
import { withInstall } from '@useful-ui/utils'

export { useSpin } from '@useful-ui/hooks'
export const UseSpin = withInstall(Spin)

export * from './src/props'

export default Spin
